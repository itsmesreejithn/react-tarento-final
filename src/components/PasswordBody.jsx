import React from "react";
import { Container } from "react-bootstrap";

const PasswordBody = () => {
  return <Container className="body-container">Passwords</Container>;
};

export default PasswordBody;
